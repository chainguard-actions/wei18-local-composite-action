name: Example

runs:
  using: "composite"
  steps:
    - uses: wei18/local-composite-action@3e6f017c4bf7f9451fc0b6d987d47507fa401aa3 # main
      with:
        composite_action_path: ${{ github.action_path }}
        composite_action_repository: ${{ github.action_repository }}

    - name: Run just-composite-action
      uses: ./../wei18/local-composite-action/.github/composite-actions/example/just-composite-action

    - name: Run another-composite-action
      uses: ./../wei18/local-composite-action/.github/composite-actions/example/just-composite-action

    - name: Finish example
      shell: bash
      run: echo "Example completed successfully"
